1. duplicate end of line alt shift down
2. move selected select and alt down

* ES6 (ES2015)

LITERAL
* Object {}
* Boolean true, false
* String '', ""
* Template (ES6) `` ${variable, expression}

VAR
* * If you use var outside of a function, it belongs to the global scope.
*If you use var inside of a function, it belongs to that function.
*If you use var inside of a block, i.e. a for loop, the variable is still available outside of that block.

LET
* let is the block scoped version of var, and is limited to the block (or expression) where it is defined.
* If you use let inside of a block, i.e. a for loop, the variable is only available inside of that loop.

CONST
* const is a variable that once it has been created, its value can never change.

NOTATION
* Dot Notation
* Bracket Notation (useful in dynamic, we can use space and special characters)

CLONING AN OBJECT
* ... => Spread operator

OOPS
1. Encapsulation
2. Abstraction
3. Inheritance
4. Polymorphism

FUNCTIONS
1. Factory Function
2. Constructor Function

THIS
* Global function refers the (this) keyword to the global window object not the parent object.
* Constructor function create a new object and display the property.

ARRAY FILTER METHOD
* Filter with call back function in an array to filter (with 3 parameters). 

MAP METHOD
* Map with call back function in an array to map (with 3 parameters). 

CHAINING
* .filter() => .map() => .filter() => .map()

NAMING
* Factory Function - Camel Notation (sudhakarThirugnanamKrishnan)
* Constructor Function - Pascal Notation (SudhakarThirugnanamKrishnan)

VALUE TYPE (Primitives)
1. Number
2. String
3. Boolean
4. Symbol (new in ES6)
5. Undefined
6. null

REFERENCE TYPE
1. Object
2. Function
3. Arrray

* Primitives (value types) are copied by their value
* Objects (reference types) are copied by their reference

ARRAY
* Array is a data structure used to represent list of items.

STRING
* includes
* startsWith
* indexOf
* toUpperCase
* trim (removes white space before and after)
* trimLeft
* split (into an array and we get with index)
* escape notation (/' /n //)

CONDITIONAL STATEMENTS
* if else
* switch case

LOOPS
* For
* While
* Do...While
* For...In
* For...of

ACCESSORS
* Getters => Access properties
* Setters => change (mutate) them

REST
* REpresentational State Transfer

HTTP PROTOCOL (CRUD Operations)
* Create
* Read
*  Update
*  Delete

HTTP METHODS
* GET              =>   for  getting data  
* POST           =>   for creating data 
* PUT              =>   for updating data
* DELETE       =>   for deleting data

NODEMON (Node Monitor npm)
* To run a js file without stopping and start every time on node cmd

ASSIGN PORT DYNAMICALLY
• process.env.PORT
